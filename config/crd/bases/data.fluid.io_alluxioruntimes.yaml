
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.3.0
  creationTimestamp: null
  name: alluxioruntimes.data.fluid.io
spec:
  group: data.fluid.io
  names:
    kind: AlluxioRuntime
    listKind: AlluxioRuntimeList
    plural: alluxioruntimes
    singular: alluxioruntime
  scope: Namespaced
  subresources:
    status: {}
  validation:
    openAPIV3Schema:
      description: AlluxioRuntime is the Schema for the alluxioruntimes API
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: AlluxioRuntimeSpec defines the desired state of AlluxioRuntime
          properties:
            alluxioVersion:
              description: The version information that instructs fluid to orchestrate
                a particular version of Alluxio.
              properties:
                image:
                  type: string
                imagePullPolicy:
                  type: string
                imageTag:
                  type: string
              type: object
            dataReplicas:
              description: The copies of the dataset
              format: int32
              type: integer
            fuse:
              description: AlluxioFuseSpec is a description of the alluxio fuse
              properties:
                args:
                  description: The mountPath in the host machine MountPath string
                    `json:"mountPath,omitempty"` Placement rook.PlacementSpec `json:"placement,omitempty"`
                  items:
                    type: string
                  type: array
                env:
                  additionalProperties:
                    type: string
                  type: object
                image:
                  type: string
                imagePullPolicy:
                  type: string
                imageTag:
                  type: string
                jvmOptions:
                  items:
                    type: string
                  type: array
                properties:
                  additionalProperties:
                    type: string
                  type: object
                resources:
                  description: ShortCircuitPolicy string            `json:"shortCircuitPolicy,omitempty"`
                    Resources are not allowed for ephemeral containers. Ephemeral
                    containers use spare resources already allocated to the pod.
                  properties:
                    limits:
                      additionalProperties:
                        anyOf:
                        - type: integer
                        - type: string
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      description: 'Limits describes the maximum amount of compute
                        resources allowed. More info: https://kubernetes.io/docs/concepts/configuration/manage-compute-resources-container/'
                      type: object
                    requests:
                      additionalProperties:
                        anyOf:
                        - type: integer
                        - type: string
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      description: 'Requests describes the minimum amount of compute
                        resources required. If Requests is omitted for a container,
                        it defaults to Limits if that is explicitly specified, otherwise
                        to an implementation-defined value. More info: https://kubernetes.io/docs/concepts/configuration/manage-compute-resources-container/'
                      type: object
                  type: object
              type: object
            jobMaster:
              description: A spec for alluxio job master
              properties:
                jvmOptions:
                  items:
                    type: string
                  type: array
                ports:
                  additionalProperties:
                    type: integer
                  type: object
                properties:
                  additionalProperties:
                    type: string
                  type: object
                replicas:
                  description: Replicas is the desired number of replicas of the given
                    template. If unspecified, defaults to 1. replicas is the min replicas
                    of dataset in the cluster
                  format: int32
                  minimum: 1
                  type: integer
                resources:
                  description: Resources are not allowed for ephemeral containers.
                    Ephemeral containers use spare resources already allocated to
                    the pod.
                  properties:
                    limits:
                      additionalProperties:
                        anyOf:
                        - type: integer
                        - type: string
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      description: 'Limits describes the maximum amount of compute
                        resources allowed. More info: https://kubernetes.io/docs/concepts/configuration/manage-compute-resources-container/'
                      type: object
                    requests:
                      additionalProperties:
                        anyOf:
                        - type: integer
                        - type: string
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      description: 'Requests describes the minimum amount of compute
                        resources required. If Requests is omitted for a container,
                        it defaults to Limits if that is explicitly specified, otherwise
                        to an implementation-defined value. More info: https://kubernetes.io/docs/concepts/configuration/manage-compute-resources-container/'
                      type: object
                  type: object
              type: object
            jobWorker:
              description: A spec for alluxio job Worker
              properties:
                jvmOptions:
                  items:
                    type: string
                  type: array
                ports:
                  additionalProperties:
                    type: integer
                  type: object
                properties:
                  additionalProperties:
                    type: string
                  type: object
                replicas:
                  description: Replicas is the desired number of replicas of the given
                    template. If unspecified, defaults to 1. replicas is the min replicas
                    of dataset in the cluster
                  format: int32
                  minimum: 1
                  type: integer
                resources:
                  description: Resources are not allowed for ephemeral containers.
                    Ephemeral containers use spare resources already allocated to
                    the pod.
                  properties:
                    limits:
                      additionalProperties:
                        anyOf:
                        - type: integer
                        - type: string
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      description: 'Limits describes the maximum amount of compute
                        resources allowed. More info: https://kubernetes.io/docs/concepts/configuration/manage-compute-resources-container/'
                      type: object
                    requests:
                      additionalProperties:
                        anyOf:
                        - type: integer
                        - type: string
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      description: 'Requests describes the minimum amount of compute
                        resources required. If Requests is omitted for a container,
                        it defaults to Limits if that is explicitly specified, otherwise
                        to an implementation-defined value. More info: https://kubernetes.io/docs/concepts/configuration/manage-compute-resources-container/'
                      type: object
                  type: object
              type: object
            jvmOptions:
              items:
                type: string
              type: array
            master:
              description: A spec for alluxio master
              properties:
                jvmOptions:
                  items:
                    type: string
                  type: array
                ports:
                  additionalProperties:
                    type: integer
                  type: object
                properties:
                  additionalProperties:
                    type: string
                  type: object
                replicas:
                  description: Replicas is the desired number of replicas of the given
                    template. If unspecified, defaults to 1. replicas is the min replicas
                    of dataset in the cluster
                  format: int32
                  minimum: 1
                  type: integer
                resources:
                  description: Resources are not allowed for ephemeral containers.
                    Ephemeral containers use spare resources already allocated to
                    the pod.
                  properties:
                    limits:
                      additionalProperties:
                        anyOf:
                        - type: integer
                        - type: string
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      description: 'Limits describes the maximum amount of compute
                        resources allowed. More info: https://kubernetes.io/docs/concepts/configuration/manage-compute-resources-container/'
                      type: object
                    requests:
                      additionalProperties:
                        anyOf:
                        - type: integer
                        - type: string
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      description: 'Requests describes the minimum amount of compute
                        resources required. If Requests is omitted for a container,
                        it defaults to Limits if that is explicitly specified, otherwise
                        to an implementation-defined value. More info: https://kubernetes.io/docs/concepts/configuration/manage-compute-resources-container/'
                      type: object
                  type: object
              type: object
            properties:
              additionalProperties:
                type: string
              type: object
            tieredstore:
              description: Tieredstore is a description of the tiered store
              properties:
                levels:
                  items:
                    properties:
                      high:
                        description: StorageType common.CacheStoreType `json:"storageType,omitempty"`
                          float64 is not supported, https://github.com/kubernetes-sigs/controller-tools/issues/245
                        type: string
                      low:
                        type: string
                      mediumtype:
                        description: Alias string `json:"alias,omitempty"`
                        enum:
                        - MEM
                        - SSD
                        - HDD
                        type: string
                      path:
                        minLength: 1
                        type: string
                      quota:
                        anyOf:
                        - type: integer
                        - type: string
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                    required:
                    - mediumtype
                    type: object
                  type: array
              type: object
            worker:
              description: A spec for alluxio worker
              properties:
                jvmOptions:
                  items:
                    type: string
                  type: array
                ports:
                  additionalProperties:
                    type: integer
                  type: object
                properties:
                  additionalProperties:
                    type: string
                  type: object
                replicas:
                  description: Replicas is the desired number of replicas of the given
                    template. If unspecified, defaults to 1. replicas is the min replicas
                    of dataset in the cluster
                  format: int32
                  minimum: 1
                  type: integer
                resources:
                  description: Resources are not allowed for ephemeral containers.
                    Ephemeral containers use spare resources already allocated to
                    the pod.
                  properties:
                    limits:
                      additionalProperties:
                        anyOf:
                        - type: integer
                        - type: string
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      description: 'Limits describes the maximum amount of compute
                        resources allowed. More info: https://kubernetes.io/docs/concepts/configuration/manage-compute-resources-container/'
                      type: object
                    requests:
                      additionalProperties:
                        anyOf:
                        - type: integer
                        - type: string
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      description: 'Requests describes the minimum amount of compute
                        resources required. If Requests is omitted for a container,
                        it defaults to Limits if that is explicitly specified, otherwise
                        to an implementation-defined value. More info: https://kubernetes.io/docs/concepts/configuration/manage-compute-resources-container/'
                      type: object
                  type: object
              type: object
          type: object
        status:
          description: AlluxioRuntimeStatus defines the observed state of AlluxioRuntime
          properties:
            cacheStates:
              additionalProperties:
                type: string
              description: CacheStatus represents the total resources of the dataset.
              type: object
            conditions:
              description: Represents the latest available observations of a ddc runtime's
                current state.
              items:
                description: Condition describes the state of the cache at a certain
                  point.
                properties:
                  lastProbeTime:
                    description: The last time this condition was updated.
                    format: date-time
                    type: string
                  lastTransitionTime:
                    description: Last time the condition transitioned from one status
                      to another.
                    format: date-time
                    type: string
                  message:
                    description: A human readable message indicating details about
                      the transition.
                    type: string
                  reason:
                    description: The reason for the condition's last transition.
                    type: string
                  status:
                    description: Status of the condition, one of True, False, Unknown.
                    type: string
                  type:
                    description: Type of cache condition.
                    type: string
                required:
                - status
                - type
                type: object
              type: array
            currentFuseNumberScheduled:
              description: The total number of nodes that can be running the runtime
                Fuse pod (including nodes correctly running the runtime Fuse pod).
              format: int32
              type: integer
            currentMasterNumberScheduled:
              description: The total number of nodes that should be running the runtime
                pod (including nodes correctly running the runtime master pod).
              format: int32
              type: integer
            currentWorkerNumberScheduled:
              description: The total number of nodes that can be running the runtime
                worker pod (including nodes correctly running the runtime worker pod).
              format: int32
              type: integer
            desiredFuseNumberScheduled:
              description: The total number of nodes that should be running the runtime
                Fuse pod (including nodes correctly running the runtime Fuse pod).
              format: int32
              type: integer
            desiredMasterNumberScheduled:
              description: The total number of nodes that should be running the runtime
                pod (including nodes correctly running the runtime master pod).
              format: int32
              type: integer
            desiredWorkerNumberScheduled:
              description: The total number of nodes that should be running the runtime
                worker pod (including nodes correctly running the runtime worker pod).
              format: int32
              type: integer
            fuseNumberAvailable:
              description: The number of nodes that should be running the runtime
                Fuse pod and have one or more of the runtime Fuse pod running and
                available (ready for at least spec.minReadySeconds)
              format: int32
              type: integer
            fuseNumberReady:
              description: The number of nodes that should be running the runtime
                Fuse pod and have one or more of the runtime Fuse pod running and
                ready.
              format: int32
              type: integer
            fuseNumberUnavailable:
              description: The number of nodes that should be running the runtime
                fuse pod and have none of the runtime fuse pod running and available
                (ready for at least spec.minReadySeconds)
              format: int32
              type: integer
            fusePhase:
              description: FusePhase is the Fuse running phase
              type: string
            fuseReason:
              type: string
            masterNumberReady:
              description: The number of nodes that should be running the runtime
                worker pod and have zero or more of the runtime master pod running
                and ready.
              format: int32
              type: integer
            masterPhase:
              description: 'INSERT ADDITIONAL STATUS FIELD - define observed state
                of cluster Important: Run "make" to regenerate code after modifying
                this file MasterPhase is the master running phase'
              type: string
            masterReason:
              type: string
            valueFile:
              type: string
            workerNumberAvailable:
              description: The number of nodes that should be running the runtime
                worker pod and have one or more of the runtime worker pod running
                and available (ready for at least spec.minReadySeconds)
              format: int32
              type: integer
            workerNumberReady:
              description: The number of nodes that should be running the runtime
                worker pod and have one or more of the runtime worker pod running
                and ready.
              format: int32
              type: integer
            workerNumberUnavailable:
              description: The number of nodes that should be running the runtime
                worker pod and have none of the runtime worker pod running and available
                (ready for at least spec.minReadySeconds)
              format: int32
              type: integer
            workerPhase:
              description: WorkerPhase is the worker running phase
              type: string
            workerReason:
              type: string
          required:
          - currentFuseNumberScheduled
          - currentMasterNumberScheduled
          - currentWorkerNumberScheduled
          - desiredFuseNumberScheduled
          - desiredMasterNumberScheduled
          - desiredWorkerNumberScheduled
          - fuseNumberReady
          - fusePhase
          - masterNumberReady
          - masterPhase
          - valueFile
          - workerNumberReady
          - workerPhase
          type: object
      type: object
  version: v1alpha1
  versions:
  - name: v1alpha1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
