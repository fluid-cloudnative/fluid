---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.8.0
  creationTimestamp: null
  name: dataprocesses.data.fluid.io
spec:
  group: data.fluid.io
  names:
    kind: DataProcess
    listKind: DataProcessList
    plural: dataprocesses
    singular: dataprocess
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DataProcess is the Schema for the dataprocesses API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: DataProcessSpec defines the desired state of DataProcess
            properties:
              dataset:
                description: TargetDataset defines which dataset will be processed
                  by DataProcess. Under the hood, the dataset's pvc will be mounted
                  to the given mountPath of the DataProcess's containers.
                properties:
                  mountPath:
                    description: MountPath defines where the Dataset should be mounted
                      in DataProcess's containers.
                    type: string
                  name:
                    description: Name defines name of the target dataset
                    type: string
                  namespace:
                    description: Namespace defines namespace of the target dataset
                    type: string
                  subPath:
                    description: SubPath defines subpath of the target dataset to
                      mount.
                    type: string
                required:
                - mountPath
                - name
                type: object
              processor:
                description: Processor defines the actual processor for DataProcess.
                  Processor can be either of a Job or a Shell script.
                properties:
                  job:
                    properties:
                      image:
                        type: string
                      script:
                        type: string
                      serviceAccountName:
                        type: string
                    required:
                    - image
                    - script
                    type: object
                type: object
            required:
            - dataset
            - processor
            type: object
          status:
            description: OperationStatus defines the observed state of operation
            properties:
              conditions:
                description: Conditions consists of transition information on operation's
                  Phase
                items:
                  description: Condition explains the transitions on phase
                  properties:
                    lastProbeTime:
                      description: LastProbeTime describes last time this condition
                        was updated.
                      format: date-time
                      type: string
                    lastTransitionTime:
                      description: LastTransitionTime describes last time the condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: Message is a human-readable message indicating
                        details about the transition
                      type: string
                    reason:
                      description: Reason for the condition's last transition
                      type: string
                    status:
                      description: Status of the condition, one of `True`, `False`
                        or `Unknown`
                      type: string
                    type:
                      description: Type of condition, either `Complete` or `Failed`
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              duration:
                description: Duration tell user how much time was spent to operation
                type: string
              infos:
                additionalProperties:
                  type: string
                description: Infos operation customized name-value
                type: object
              phase:
                description: Phase describes current phase of operation
                type: string
            required:
            - conditions
            - duration
            - phase
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
